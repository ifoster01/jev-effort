/**
 * Merge overlapping or touching closed intervals.
 *
 * @param {Array<[number, number]>} intervals pairs [start, end] with start <= end, in any order
 * @returns {Array<[number, number]>} merged intervals sorted by start
 *
 * Touching intervals merge: [1, 2] and [2, 3] become [1, 3].
 * The input array and its pairs must not be modified.
 */
function mergeIntervals(intervals) {
  throw new Error("not implemented");
}

module.exports = { mergeIntervals };
